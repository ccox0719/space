cd /Users/cox/Documents/Cosmo
./scripts/backup.sh

netlify deploy --prod

git add .
git commit -m "update"
git push

npm run export

"scripts": {
  "publish": "npm run export-pages && git add package.json scripts docs/content cheatsheet.txt && git commit -m \"update\" && git push"
}
